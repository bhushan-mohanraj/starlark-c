# Starlark in C

An embeddable implementation
of the Starlark language in C.

The existing implementations
are in Java and Go (Google) and in Rust (Meta).
This implementation seeks to provide
a portable and compliant interpreter for C applications.
